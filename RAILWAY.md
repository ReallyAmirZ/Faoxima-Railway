# نصب Faoxima روی Railway — بسته ۲۰۲۶/۰۹/۲۵

سورس اصلی: نسخه 1.1.1، کامیت `8dddaa09fc76fb22699f8f1e4b7a4c2bb0619012`.
فورک مرجع: `x4gpanell/Faoxima`، کامیت `995a861659db66b2ca50e8d01a6a738b2c6f359b`.

این بسته ربات، پنل وب و مینی‌اپ نسخه جدید را با PHP 8.2 و Apache اجرا می‌کند. پورت، تنظیمات دیتابیس و وب‌هوک هنگام شروع ساخته می‌شوند. نصب از Variables است؛ به `/installer` مراجعه نکنید.

## اگر همین حالا ربات فعال دارید

1. از دیتابیس فعلی خروجی SQL بگیرید و فایل‌های آپلودی و لوگوی خود را نگه دارید.
2. نام سرویس MySQL و Variables فعلی ربات را یادداشت کنید.
3. این بسته را به مخزن متصل به Railway منتقل کنید؛ فایل‌های هم‌نام باید جایگزین شوند. خود ZIP را آپلود نکنید. برای انتقال تعداد زیاد فایل‌ها می‌توانید از GitHub Desktop استفاده کنید.
4. اگر مخزن خیلی قدیمی است، روش تمیزتر این است که از سورس اصلی یک Fork جدید بسازید و فایل‌های بسته «Railway overlay» را در ریشه‌ی آن جایگزین کنید. سپس Source سرویس موجود Railway را به همان مخزن تغییر دهید.
5. سرویس MySQL و Volume آن را نگه دارید. اگر سرویس ربات جدیدی می‌سازید، ابتدا ربات قبلی را متوقف کنید تا کرون‌های دو نسخه هم‌زمان روی یک دیتابیس کار نکنند.
6. Variables بخش بعد را تنظیم کنید و Deploy بزنید. ساختار دیتابیس با `table.php` نسخه اصلی به‌روزرسانی می‌شود؛ این اسکریپت تغییرات واقعی روی داده/ساختار انجام می‌دهد، بنابراین بکاپ پیش از ارتقا ضروری است.

بازگشت به نسخه قبلی کد لزوماً دیتابیس را به نسخه قبلی برنمی‌گرداند. بکاپ SQL را تا پایان آزمایش نگه دارید.

## نصب تازه، قدم به قدم

1. از `https://github.com/Mmd-Amir/Faoxima` یک Fork بسازید. بسته overlay برای کامیت درج‌شده بالاست؛ روی نسخه‌های بعدی بدون بررسی مجدد استفاده نکنید.
2. فایل overlay را از حالت فشرده خارج کنید. محتویات آن را در ریشه‌ی Fork قرار دهید؛ فایل `Dockerfile` باید کنار `index.php` باشد، نه داخل یک پوشه‌ی اضافه. پوشه‌های `docker`، `re` و `api` باید با پوشه‌های موجود ادغام شوند.
3. تغییرات را Commit کنید. بسته کامل نیز جایگزین همین دو مرحله است و تمام سورس را دارد.
4. در Railway یک Project بسازید و یک سرویس MySQL اضافه کنید. اگر سرویس MySQL از قبل دارید، همان را استفاده کنید.
5. سرویس ربات را از GitHub Repository بسازید. در Settings تنظیمات قدیمی Build Command و Start Command را پاک کنید تا Dockerfile و ENTRYPOINT این بسته اجرا شوند. Root Directory باید ریشه‌ی پروژه باشد.
6. از Networking برای سرویس ربات Generate Domain بزنید. پورت مقصد را `8080` قرار دهید و در Variables مقدار `PORT=8080` بگذارید.
7. در Variables سرویس ربات مقادیر زیر را قرار دهید؛ اگر سرویس دیتابیس نام دیگری دارد، `MySQL` را در Referenceها تغییر دهید:

```text
PORT=8080
DB_HOST=${{MySQL.MYSQLHOST}}
DB_NAME=${{MySQL.MYSQLDATABASE}}
DB_USER=${{MySQL.MYSQLUSER}}
DB_PASS=${{MySQL.MYSQLPASSWORD}}
TELEGRAM_BOT_TOKEN=توکن_ربات
TELEGRAM_ADMIN_ID=آیدی_عددی_ادمین
FAOXIMA_AUTO_MIGRATE=1
```

این تنظیمات برای MySQL داخلی Railway با پورت استاندارد 3306 است. `DB_HOST` فقط نام میزبان است؛ در آن `https://` ننویسید.

`DOMAIN` را برای دامنه تولیدشده Railway خالی/حذف کنید؛ برنامه از `RAILWAY_PUBLIC_DOMAIN` استفاده می‌کند. برای دامنه اختصاصی، `DOMAIN=bot.example.com` قرار دهید. `URL_PATH` را خالی/حذف کنید؛ ربات در ریشه دامنه اجرا می‌شود.

`TELEGRAM_WEBHOOK_SECRET` اختیاری است؛ پیش‌فرض پایدار از توکن ساخته می‌شود. مقدار دستی باید ۱ تا ۲۵۶ کاراکتر از حروف انگلیسی، اعداد، `_` یا `-` باشد. `TELEGRAM_BOT_USERNAME` نیز اختیاری و بدون `@` است.

8. Deploy کنید. اگر اولین Deploy به دلیل نبودن Variables شکست خورد، بعد از تکمیل آن‌ها Redeploy کنید.
9. در Deploy Logs این دو پیام را بررسی کنید:

```text
[railway] Telegram accepted webhook registration.
[railway] Faoxima is ready on port 8080.
```

10. آدرس `https://YOUR-DOMAIN/health.php` باید پاسخ `{"ok":true,"service":"faoxima"}` بدهد. این تست فقط زنده‌بودن وب‌سرور را نشان می‌دهد؛ سپس `/start`، یک سرویس تست و تحویل لینک/QR را در تلگرام آزمایش کنید.
11. پنل وب: `https://YOUR-DOMAIN/panel/` و مینی‌اپ: `https://YOUR-DOMAIN/app/`. احراز هویت و فعال‌سازی آن‌ها از تنظیمات خود ربات انجام می‌شود.

## نگهداری اطلاعات و بکاپ

داده کاربران و سفارش‌ها در MySQL هستند؛ Volume دیتابیس را حذف نکنید. فایل‌هایی که داخل کانتینر ربات ساخته یا آپلود می‌شوند بدون Volume پایدار نیستند. برای پوشه `storage` می‌توانید Volume را روی `/var/www/faoxima/storage` وصل کنید؛ فایل‌های خارج از این مسیر، از جمله لوگوی ریشه، با این Volume نگهداری نمی‌شوند. لوگوی این بسته همان فایل شخصی موجود در نسخه Railway قبلی شماست؛ تغییر لوگوی ریشه را در مخزن Commit کنید.

برای انتقال، خروجی SQL را در دیتابیس مقصد Import کنید، ربات قبلی را متوقف کنید و سپس نسخه جدید را با Variables مقصد اجرا کنید. توکن و رمزها را در GitHub یا ZIP بکاپ عمومی قرار ندهید.

## رفع اشکال

- `Missing required variables`: Referenceها و نام سرویس MySQL را بررسی کنید.
- `Database was not ready`: سرویس دیتابیس، رمز و شبکه خصوصی پروژه را بررسی کنید.
- خطای وب‌هوک: توکن و دامنه HTTPS را بررسی کنید. شروع برنامه در صورت ردشدن ثبت وب‌هوک متوقف می‌شود.
- ساخت سرویس بدون تحویل: این بسته خطای نادیده‌گرفتن پاسخ ناموفق ارسال عکس را اصلاح می‌کند. اگر عکس رد شود، پیام متنی همراه دکمه‌های دریافت ارسال می‌شود. شکست خود پیام متنی یا خطای پنل نیازمند بررسی لاگ همان سفارش است.
- آپدیت بعدی: از روش داخل‌ربات مخصوص VPS استفاده نکنید؛ بسته Railway را از سورس جدید بسازید و از GitHub دوباره Deploy کنید.

راهنمای رسمی Railway: [Dockerfile](https://docs.railway.com/builds/dockerfiles)، [Volume](https://docs.railway.com/volumes).
