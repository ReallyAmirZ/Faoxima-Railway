# اصلاح خطای ۲۰۱ و کرون — ۲۰۲۶/۰۹/۲۵، اصلاحیه دوم

این اصلاحیه برای بسته Railway نسخه 1.1.1 منتشرشده در همین تاریخ است.

## نصب

۱. از دیتابیس فعلی بکاپ SQL بگیرید.
۲. فایل ZIP اصلاحیه را استخراج و محتویات آن را با حفظ مسیرها در ریشه مخزن خود جایگزین کنید:

```text
panels.php
Dockerfile
docker/faoxima.cron
cron/cron.php
```

۳. تغییرات را Commit و در Railway نسخه جدید را Deploy کنید. Variables و MySQL را تغییر ندهید. اجرای Docker build برای اعمال فایل cron ضروری است؛ Restart تنها، فایل‌های ایمیج را عوض نمی‌کند.
۴. یک سرویس تست رایگان بسازید. دیگر نباید پاسخ موفق HTTP 201 به‌عنوان خطای ساخت گزارش شود. `runuser: not found` نیز باید از لاگ کرون حذف شود.

## دلیل و اصلاح

- مرزبان فقط HTTP 200 را قبول می‌کرد و موفقیت HTTP 201 را خطا گزارش می‌داد. مرزبان، پاسارگارد و X-UI اکنون 200/201 را می‌پذیرند و محتوای پاسخ نیز اعتبارسنجی می‌شود؛ 202، 401، 409 و پاسخ خراب موفق محسوب نمی‌شوند.
- مرزبان با version_panel=1 می‌توانست آرایه links را مستقیم به explode بدهد. خواندن اشتراک اکنون یک بار انجام می‌شود و اگر خواندن آن شکست بخورد لینک‌های موجود حفظ می‌شوند.
- کرون PATH محدودی داشت؛ runuser با مسیر کامل /usr/sbin/runuser و PHP با /usr/local/bin/php اجرا می‌شود. Docker build وجود هر دو را بررسی می‌کند.
- تعریف قدیمی iranpay1.php هنوز در فهرست کرون هست ولی فایلش در سورس نیست. زمان‌بند اکنون فایل‌های ناموجود را اجرا نمی‌کند. این تغییر درگاه ایران‌پی را پیاده‌سازی نمی‌کند.

## سفارش قبلاً پرداخت‌شده

این اصلاحیه به سفارش‌های قدیمی، موجودی کیف پول یا پرداخت‌ها دست نمی‌زند و تحویل خودکار مجدد ندارد. پیش از هر اقدام، فاکتور و نام کاربری کانفیگ را با کاربر تطبیق دهید و بررسی کنید آیا مبلغ به کیف پول برگشته است. اگر سرویس در پنل موجود است، آن را دوباره نسازید. پس از روشن‌شدن وضعیت حساب، همان لینک اشتراک موجود را برای صاحب سفارش ارسال کنید؛ اصلاح ثبت فاکتور/موجودی نیاز به بررسی همان رکوردها دارد.

## آزمایش

خطای ۲۰۱ در تابع createUser سورس قبلی بازتولید شد. نسخه اصلاح‌شده ۲۱ سناریوی پاسخ پنل را در PHP 8.2.33 با پاسخ‌های شبیه‌سازی‌شده پاس کرد. سناریوها شامل 200، 201، JSON خراب، نبود username، خطاهای 401/409/202 و اشتراک base64 هستند؛ در هر سناریو فقط یک درخواست ایجاد کاربر اجرا شد. تست‌های قبلی تحویل و بررسی نحو PHP نیز پاس شدند. اجرای واقعی روی Railway و تسویه سفارش قبلی انجام نشده‌اند.
