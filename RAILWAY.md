# اجرای Faoxima روی Railway

این شاخه از Docker استفاده می‌کند و هنگام هر استقرار، تنظیمات را از Variables می‌سازد، جداول دیتابیس را به‌روز می‌کند، وب‌هوک تلگرام را تنظیم می‌کند و کرون داخلی را هر دقیقه اجرا می‌کند.

## 1. ساخت سرویس‌ها

1. در Railway یک پروژه بسازید.
2. از `+ New` یک سرویس **MySQL** اضافه کنید.
3. مخزن GitHub این فورک را به‌عنوان سرویس دوم اضافه کنید.
4. برای سرویس ربات از بخش Networking یک دامنه بسازید.

## 2. Variables سرویس ربات

مقادیر زیر را در سرویس ربات وارد کنید. اگر نام سرویس دیتابیس شما `MySQL` نیست، همان نام واقعی را در Referenceها قرار دهید.

```text
DB_HOST=${{MySQL.MYSQLHOST}}
DB_NAME=${{MySQL.MYSQLDATABASE}}
DB_USER=${{MySQL.MYSQLUSER}}
DB_PASS=${{MySQL.MYSQLPASSWORD}}
TELEGRAM_BOT_TOKEN=توکن_ربات
TELEGRAM_ADMIN_ID=آیدی_عددی_ادمین
FAOXIMA_AUTO_MIGRATE=1
```

`DOMAIN` و `PORT` را دستی نسازید؛ Railway آن‌ها را در زمان اجرا فراهم می‌کند. برنامه از `RAILWAY_PUBLIC_DOMAIN` استفاده می‌کند و در ریشه دامنه اجرا می‌شود.

متغیر `TELEGRAM_WEBHOOK_SECRET` اختیاری است. اگر تنظیم نشود، برنامه به‌صورت امن آن را از توکن ربات تولید می‌کند.

## 3. استقرار

پس از ثبت Variables، Deploy را اجرا کنید. پایان صحیح لاگ باید شامل این پیام باشد:

```text
[railway] Faoxima is ready on port ...
```

آدرس زیر نیز باید پاسخ `{"ok":true,"service":"faoxima"}` بدهد:

```text
https://YOUR-DOMAIN/health.php
```

سپس در تلگرام `/start` را ارسال کنید. راه‌اندازی جداول و تنظیم وب‌هوک در هر استقرار به‌صورت خودکار و امن تکرار می‌شود.

## انتقال دیتابیس قبلی

قبل از اولین Deploy، فایل SQL بکاپ را داخل دیتابیس MySQL جدید Import کنید. اجرای خودکار `table.php` داده‌های موجود را پاک نمی‌کند و فقط ساختار لازم برای نسخه جدید را ایجاد یا به‌روز می‌کند. برای جلوگیری از تداخل، سرویس قبلی را پس از موفق شدن سرویس جدید متوقف کنید.
